import { getFrequencyLabel } from "./utils";

/**git-and-github-workflow  - version 5.13 - utils > index
 * - Features: 
 * 
 *     --> Importing and exporting 'getFrequencyLabel'
 *   
 * Note: In order to implement the search 'filterCommands'
 * makes a new array from 'gitCommands' and then show the 
 * results from the 'value' entered by the user 
 */

export { getFrequencyLabel }