import { getFrequencyLabel, handleCopyCommand } from "./utils";

/**git-and-github-workflow  - version 8.01 - utils > index
 * - Features: 
 * 
 *     --> Importing and exporting 'handleCopyCommand'
 *   
 * Note: In order to implement the search 'filterCommands'
 * makes a new array from 'gitCommands' and then show the 
 * results from the 'value' entered by the user 
 */

export { getFrequencyLabel, handleCopyCommand }